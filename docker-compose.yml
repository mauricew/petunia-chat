services:
  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_USER: petunia
      POSTGRES_PASSWORD: petunia
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  postgres_data: